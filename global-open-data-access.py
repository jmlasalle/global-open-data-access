# dataset class
class dataset(object):
    """docstring for ."""

    def __init__(self, arg):
        super(, self).__init__()
        self.arg = arg


# about
def about(name):

    return dataset_meta

# download
def download(name, save_path):

    if save_path not None:
        #save dataset to disk
    else:

        return data
